services:
  - type: web
    name: nl2sql-app
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python setup_database.py && python seed_memory.py && uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
